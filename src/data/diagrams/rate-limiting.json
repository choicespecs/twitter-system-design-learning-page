{
  "basic": {
    "nodes": [
      {
        "id": "client",
        "label": "Client",
        "x": 110,
        "y": 90,
        "description": "Sends requests to the API."
      },
      {
        "id": "api",
        "label": "API",
        "x": 330,
        "y": 90,
        "description": "Single server checking its own local counter."
      },
      {
        "id": "counter",
        "label": "In-Memory Counter",
        "x": 550,
        "y": 90,
        "description": "Resets every fixed window (e.g. once per minute). Doesn't share state across servers."
      }
    ],
    "edges": [
      {
        "from": "client",
        "to": "api"
      },
      {
        "from": "api",
        "to": "counter"
      }
    ]
  },
  "scaled": {
    "nodes": [
      {
        "id": "client",
        "label": "Client",
        "x": 110,
        "y": 90,
        "description": "Sends requests to the API."
      },
      {
        "id": "gateway",
        "label": "API Gateway",
        "x": 330,
        "y": 90,
        "description": "Checks the shared limit before routing to a backend server."
      },
      {
        "id": "redis",
        "label": "Redis Sliding Window",
        "x": 550,
        "y": 90,
        "description": "Shared counter across all servers; sliding window avoids the fixed-window boundary burst."
      }
    ],
    "edges": [
      {
        "from": "client",
        "to": "gateway"
      },
      {
        "from": "gateway",
        "to": "redis"
      }
    ]
  },
  "advanced": {
    "nodes": [
      {
        "id": "client",
        "label": "Client",
        "x": 110,
        "y": 90,
        "description": "Sends requests."
      },
      {
        "id": "edge",
        "label": "Edge Rate Limiter",
        "x": 330,
        "y": 90,
        "description": "Rejects abusive traffic before it reaches backend servers."
      },
      {
        "id": "bucket",
        "label": "Token Bucket (Redis)",
        "x": 550,
        "y": 90,
        "description": "Refills at a steady rate; allows short bursts up to the bucket size."
      },
      {
        "id": "tiering",
        "label": "Tiered Limits",
        "x": 770,
        "y": 55,
        "description": "Different limits per endpoint — writes stricter than reads."
      },
      {
        "id": "api",
        "label": "Backend API",
        "x": 770,
        "y": 165,
        "description": "Only sees traffic that passed the limiter."
      }
    ],
    "edges": [
      {
        "from": "client",
        "to": "edge"
      },
      {
        "from": "edge",
        "to": "bucket"
      },
      {
        "from": "bucket",
        "to": "tiering"
      },
      {
        "from": "tiering",
        "to": "api"
      }
    ]
  }
}
